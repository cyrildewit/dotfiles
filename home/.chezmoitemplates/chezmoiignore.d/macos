# Ignored only on macOS (darwin).

# ~/AppData is a Windows location. The Windows Terminal and herdr configs under
# it are the only things in the source tree that target it.
AppData

# The PowerShell profile is loaded on Windows. macOS runs zsh, which reads
# .aliases instead.
.config/powershell
